let card = document.querySelectorAll("div.card");

card.forEach(card => {
    card.addEventListener("click", event=>{
        console.log(card);
        if (div.card[0] == div[4]) {
            
        }
    })
});